# ============================================================
# Environment Configuration - Acrobaticz Elite Setup
# 
# Copy this file to .env and update values for your deployment
# 
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up -d
# ============================================================

# ============================================================
# Application Environment
# ============================================================
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# ============================================================
# Database Configuration (PostgreSQL)
# ============================================================
# Credentials used by PostgreSQL container
DB_NAME=acrobaticz
DB_USER=acrobaticz_user
DB_PASSWORD=change_me_strong_password_123

# Full connection string (constructed from above in docker-entrypoint)
# DATABASE_URL=postgresql://acrobaticz_user:change_me_strong_password_123@postgres:5432/acrobaticz?schema=public

# ============================================================
# JWT Authentication
# ============================================================
# Generate with: openssl rand -base64 32
JWT_SECRET=please_change_this_secret_key_in_production_123
JWT_EXPIRATION=7d

# ============================================================
# MinIO / S3 Object Storage Configuration
# ============================================================

# MinIO Admin Credentials (for bucket management)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin_change_me_123

# Storage Path (local or external disk)
# Examples:
#   ./storage/minio                              (relative - local)
#   /var/lib/acrobaticz/storage                  (absolute - local)
#   /mnt/external-drive/acrobaticz-storage       (external disk)
#   /media/disk/backup/acrobaticz                (NAS/external)
STORAGE_PATH=./storage/minio

# MinIO Endpoint (internal to Docker network)
MINIO_ENDPOINT=http://minio:9000

# S3 API Credentials (same as MinIO admin for internal use)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin_change_me_123
S3_BUCKET=acrobaticz
S3_REGION=us-east-1

# ============================================================
# Domain & HTTPS Configuration
# ============================================================
DOMAIN=localhost:3000

# Optional: For Let's Encrypt SSL/TLS (see PRODUCTION_DEPLOYMENT.md)
# CERT_EMAIL=admin@yourdomain.com
# ENABLE_HTTPS=false

# ============================================================
# Translation API (Optional - DeepL)
# ============================================================
# Get API key from: https://www.deepl.com/pro-api
DEEPL_API_KEY=

# ============================================================
# AI/ML Integration (Optional - Google Gemini)
# ============================================================
# Get API key from: https://ai.google.dev/
# GEMINI_API_KEY=

# ============================================================
# Logging & Monitoring (Optional)
# ============================================================
# LOG_LEVEL=info
# LOG_FILE=/app/logs/app.log

# ============================================================
# Feature Flags (Optional)
# ============================================================
# ENABLE_ADVANCED_REPORTING=true
# ENABLE_PAYMENT_INTEGRATION=false
# ENABLE_EMAIL_NOTIFICATIONS=true

# ============================================================
# Database Seeding Configuration
# ============================================================
# Controls automatic database seeding on Docker startup
# The seed script uses data from CATALOG_65_PRODUTOS/

# Auto-seed when database is empty (recommended for dev)
# Set to "true" to automatically seed the database with:
# - 3 Users (Admin, Manager, Technician)
# - 1 Client (VRD Production)
# - 1 Partner
# - 6 Categories + 21 Subcategories
# - 65 Products with images
SEED_ON_START=true

# Force clean seed on every startup (WARNING: deletes all data!)
FORCE_SEED=false

# Clean database before seeding (only used with SEED_ON_START or FORCE_SEED)
SEED_CLEAN=false

# Show verbose seed output
SEED_VERBOSE=false

# Skip migrations on startup (not recommended)
# SKIP_MIGRATIONS=false

# ============================================================
# Performance & Security
# ============================================================
# Request timeout
# REQUEST_TIMEOUT=30000

# Session configuration
SESSION_SECRET=please_change_this_session_secret_also_123

# ============================================================
# Docker Compose Specific (Nginx/Reverse Proxy)
# ============================================================
# Used by Nginx configuration template
# DOMAIN=localhost

# ============================================================
# Notes for Production Deployment
# ============================================================
# 1. Change all passwords and secrets to strong values
# 2. Use environment-specific values for each deployment
# 3. Store secrets securely (Docker secrets, Vault, etc.)
# 4. Enable HTTPS for production
# 5. Configure backup strategy (see PRODUCTION_DEPLOYMENT.md)
# 6. Set up monitoring and logging
# 7. Review security checklist (see DOCKER_GUIDE.md)
# DuckDNS setup for dynamic domain
DOMAIN="acrobaticzrental.duckdns.org"
DUCKDNS_DOMAIN="acrobaticzrental"
DUCKDNS_TOKEN="your-duckdns-token-here"

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================
# Cloud storage paths (for backup drive or external storage)
EXTERNAL_STORAGE_PATH="/mnt/backup_drive/av-rentals/cloud-storage"
EXTERNAL_STORAGE_TEMP="/mnt/backup_drive/av-rentals/cloud-storage/temp"
STORAGE_CHECK_INTERVAL="300000"
DEFAULT_STORAGE_QUOTA="53687091200"
ENABLE_STORAGE_DISK_CHECK="true"

# =============================================================================
# LOGGING
# =============================================================================
LOG_FILE="false"
TZ="UTC"

# =============================================================================
# DEVELOPMENT OPTIONS
# =============================================================================
# Enable detailed logging (development only)
DEBUG="false"

# API timeout (in milliseconds)
API_TIMEOUT="30000"
