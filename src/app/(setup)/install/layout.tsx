export const metadata = {
  title: "AV Rentals Installation",
  description: "Setup wizard for AV Rentals",
};

export default function InstallLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
